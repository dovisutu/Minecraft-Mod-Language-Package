这是一个条件拼图。[link{pneumaticcraft:progwidget/conditions}]点击这里[link{}]来获取关于条件拼图的一般信息。

条件：方块允许你检查给定区域内是否有给定方块。例如，可以用此检查一个区域内是否有成熟的作物。可以通过连接物品过滤器来过滤有效方块。无人机将会检查一个方块是否可以被精准采集，如果是，其将会检查该方块是否通过物品过滤器。如果否，该方块的掉落物将会被检查。例如，可以把过滤器设为小麦，这样，当小麦作物会掉落小麦时（即成熟），其将通过过滤器。