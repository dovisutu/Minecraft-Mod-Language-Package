编程拼图被用于[link{block/programmer}]编程器[link{}]中。

[prefix{l}]跳转[prefix{}]
该拼图仅用于程序流。当程序到达跳转后，将会查看所连接的文本拼图，试图跳转到一个有同样文本的[link{pneumaticcraft:progwidget/label}]标签[link{}]。如果失败，程序流将回到开始拼图。（此缺省行为正常情况下不应该发生）

因此，跳转拼图可以用来合并程序流中的多个分支（使用多个跳转到同一标签的跳转拼图），或者仅仅通过拆分拼图的方式整理拼图。小窍门：勾选右下角的“显示程序流”；其将把有同一名称的跳转/条件/标签连接起来。